apiVersion: v1
kind: Service
metadata:
  name: nodejs-api-service
  labels:
    app: nodejs-api
spec:
  type: LoadBalancer
  selector:
    app: nodejs-api
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  sessionAffinity: None
